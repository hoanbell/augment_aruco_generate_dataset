# augment_aruco_generate_dataset
Create dataset from generated aruco, adding some augmentation such as: Noise, Blur, random Shear, Lighting (bright, subtract). And get the augment coordinates (bbox points)
